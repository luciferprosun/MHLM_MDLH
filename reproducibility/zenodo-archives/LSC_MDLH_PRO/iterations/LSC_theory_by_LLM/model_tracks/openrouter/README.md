# OpenRouter Folder

Purpose:

Track outputs produced via OpenRouter (routing/wrapper). Always record the *underlying model name* in the filename or notes when known.

Use this folder for:

- scripts used to call OpenRouter
- model configuration notes
- outputs that need attribution to a specific model

